// As ações da sincronização podem levar alguns segundos (Jetimob + fotos)
export const maxDuration = 60;

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
